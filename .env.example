# Environment variables for Supabase configuration
# Copy this file to .env.local for local development

# Supabase Configuration
# For local development (when running `npx supabase start`):
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your-local-anon-key-from-supabase-start

# For production deployment:
# VITE_SUPABASE_URL=https://your-project-ref.supabase.co
# VITE_SUPABASE_ANON_KEY=your-production-anon-key

# Optional: If you need to specify the project ID
# VITE_SUPABASE_PROJECT_ID=your-project-id

# Note: API keys for Supabase Edge Functions (like OPENAI_API_KEY and HF_TOKEN)
# should be configured in supabase/config.toml for local development
# and in your Supabase project settings for production
